# Archivo de ejemplo de variables de entorno
# Copia este archivo a .env.properties en la raíz del proyecto y completa con tus valores reales
# IMPORTANTE: .env.properties está en .gitignore y NO debe subirse al repositorio

# Puerto del servidor
PORT=8080

# Configuración de base de datos MySQL
DB_URL=jdbc:mysql://localhost:3306/space_auth?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=tu_password_aqui

# Secretos JWT (usa un valor seguro y aleatorio en producción)
JWT_SECRET=tu_secreto_jwt_muy_largo_y_seguro_aqui_minimo_256_bits

# Credenciales de administrador
ADMIN_USERNAME=admin
ADMIN_PASSWORD=tu_password_admin_seguro

# Perfil activo (dev, prod, test)
# Nota: Spring Boot leerá esta variable y la mapeará a spring.profiles.active
SPRING_PROFILES_ACTIVE=dev

# Modo debug (true/false)
DEBUG=false

